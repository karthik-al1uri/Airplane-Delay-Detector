azure-ai-ml
azure-identity
matplotlib # Added
seaborn    # Added
pandas     # Added (might be implicitly installed already, but good to list)
numpy      # Added (might be implicitly installed already, but good to list)
scikit-learn==1.3.2 # Added - IMPORTANT: Use the same version as Colab if possible
joblib     # Added (might be implicitly installed already, but good to list)